<div class="row justify-content-center mt-3">
  <div class="col col-md-6">
    <h1><%= t('.header', badge_name: @badge.name) %></h1>

    <%= render 'form' %>
    <%= link_to t('navigation.back'), admin_badge_path(@badge), class: "btn btn-primary mt-3" %>
  </div>
</div>
