<tr>
  <td><%= question.body %></td>
  <td><%= link_to t('actions.show'), admin_question_path(question) %></td>
  <td><%= link_to t('actions.edit'), edit_admin_question_path(question) %></td>
  <td>
    <%= link_to t('actions.delete'), admin_question_path(question),
                method: :delete,
                data: { confirm: t('actions.delete_confirmation') } %>
  </td>
</tr>
