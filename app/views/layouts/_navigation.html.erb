<% if user_signed_in? %>
  <p class="nav user">Welcome, <b><%= current_user_name %></b> Guru |
    <%= link_to 'Log Out', destroy_user_session_path, method: :delete %></p>
<% else %>
  <p class="nav user">
    <%= link_to 'Log In', new_user_session_path %> or
    <%= link_to 'Sign Up', new_user_registration_path %>
  </p>
<% end %>

<hr>
